package com.jwt.tok.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.jwt.tok.model.SideType;

public interface SideTypeRepository extends JpaRepository<SideType, Long> {
}